services:
  - type: web
    name: hajimi
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PASSWORD
        value: 24591800Aa
      - key: GEMINI_API_KEYS
        value: 你的GeminiAPI密钥
